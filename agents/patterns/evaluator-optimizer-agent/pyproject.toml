[project]
name            = "evaluator-optimizer-agent"
version         = "0.1.0"
description     = "Stub: evaluator-optimizer workflow. See README.md."
readme          = "README.md"
requires-python = ">=3.12"
dependencies    = []

[build-system]
requires      = ["hatchling>=1.27"]
build-backend = "hatchling.build"


[tool.hatch.build.targets.wheel]
packages = ["src/evaluator_optimizer_agent"]
