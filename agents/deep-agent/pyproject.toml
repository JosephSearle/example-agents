[project]
name            = "deep-agent"
version         = "0.1.0"
description     = "Stub: tier 3 deepagents-based pattern. See README.md."
readme          = "README.md"
requires-python = ">=3.12"
dependencies    = []

[build-system]
requires      = ["hatchling>=1.27"]
build-backend = "hatchling.build"


[tool.hatch.build.targets.wheel]
packages = ["src/deep_agent"]
