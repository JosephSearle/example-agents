{"inputs": {"question": "What framework tier does react-agent use, and separately, what tier does swarm-agent use, and how do those tiers differ?"}}
{"inputs": {"question": "How does the supervisor pattern route work versus how does the routing-agent pattern route work, and what's the key difference between the two?"}}
{"inputs": {"question": "What credential does this repo need to call chat models, and what credential (if any) does the reranker in retrieve-rerank-agent need?"}}
{"inputs": {"question": "How does map-reduce-agent decide how many workers to spawn, and how does orchestrator-workers-agent decide what work to delegate?"}}
{"inputs": {"question": "What does the evaluator-optimizer pattern's loop terminate on, and how is that different from corrective-rag-agent's retry cap?"}}
